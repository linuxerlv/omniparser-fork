# parser-demo

Single-page Gradio demo for `omniparser-core`. This is a runtime app, not
a library; it is not intended for PyPI publication (`Private :: Do Not Upload`).

Migration target for `upstream/gradio_demo.py`.

## Status

`0.1.0.dev0` — skeleton only.
