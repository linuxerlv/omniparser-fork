# omniparser-eval

Benchmark harnesses (ScreenSpot-Pro, etc.) for OmniParser.

Migration target for `upstream/eval/ss_pro_gpt4o_omniv2.py`. The 1.16 MB
result log `upstream/eval/logs_sspro_omniv2.json` is **not** migrated;
benchmark outputs will be published as GitHub release artifacts and not
committed to git history.

## Status

`0.1.0.dev0` — skeleton only.
